window.addEventListener('DOMContentLoaded', () => {
  // Example: communicate with Electron backend if needed
  // This is where you’d add logic to call a local API, or your python backend if running
});
